# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Database ORM
sqlalchemy==2.0.35

# PostgreSQL driver (for Supabase deployment)
# asyncpg will be used when deploying to Supabase
# For local dev, we use SQLite (no extra driver needed)

# Authentication
PyJWT==2.9.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.0

# File uploads
python-multipart==0.0.9

# Environment variables
python-dotenv==1.0.1

# CORS
aiofiles==24.1.0
